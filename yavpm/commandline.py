from yavpm import commands
# Contains all the command-line arguments.
# TODO(gloat): Do we process the args beforehand?
#  If so, they must be passed in, otherwise we can
#  use the below import.
from sys import argv

##takes the arguments provided from command line
##possibly just send in the list from main...
